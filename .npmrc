auto-install-peers=false
strict-peer-dependencies=false

# Skip pnpm's pre-run dependency re-verification. On Windows the automatic
# re-install it triggers runs the root preinstall guard in a subprocess where
# npm_config_user_agent is not propagated, which aborts every `pnpm run`.
verify-deps-before-run=false
